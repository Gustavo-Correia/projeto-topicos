Flat collection of Markdown documents describing the sibling `lib/` source tree; no executable code. `ARCHITECTURE.md` is the canonical deep-dive (file inventory, layer-by-layer breakdown with Mermaid diagrams, dependency graph, test coverage summary). `README.md` provides a quick-start overview and run commands. `UI_GUIDE.md`, `IOS_GUIDE.md`, `VALIDATION.md`, `STRENGTHS_IMPROVEMENTS.md`, and `TASKS.md` are topic-specific supplements. `reference/CLAUDE_PROMPT.md` is an AI-agent bootstrap prompt that enumerates which docs to read first and lists project priorities/restrictions — it is intentionally separate from the main README so it does not clutter the repo front page.