Flutter Material 3 (`useMaterial3: true`), estado via `provider` (`ChangeNotifierProvider`/`Consumer`), persistência via `hive_flutter` com `jsonEncode`/`jsonDecode` manual sobre chaves `Box<String>`, geração de UUID via `uuid`, locale definido para `pt_BR` através de `intl`, HTTP via pacote `http` para consumo REST (taxa de câmbio USD→BRL da AwesomeAPI e taxa Selic anual do SGS do Banco Central) e navegação por rotas nomeadas via `onGenerateRoute` central.
